[package]
name = "oynk-settlement-protocol-contract"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
soroban-sdk = { workspace = true }
oynk-sdk = { path = "../../packages/oynk-sdk" }

[dev-dependencies]
soroban-sdk = { workspace = true, features = ["testutils"] }
